{
  "name": "rnd-unified-platform",
  "private": true,
  "description": "Unified R&D Management Platform — frontend (React) + backend (Express)",
  "scripts": {
    "dev": "concurrently -n frontend,backend -c cyan,magenta \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "install:all": "npm install && npm install --prefix frontend --legacy-peer-deps && npm install --prefix backend",
    "start": "npm run start --prefix backend",
    "build": "npm run build --prefix frontend",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/config-conventional": "^21.1.0",
    "@conventional-changelog/git-client": "^3.0.1",
    "commitlint": "^21.1.0",
    "concurrently": "^9.2.0",
    "husky": "^9.1.7"
  },
  "overrides": {
    "shell-quote": "^1.8.4"
  }
}
